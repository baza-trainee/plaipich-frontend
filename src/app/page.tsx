import { Slider } from "@/components";

const Home = () => {
  return (
    <main className="w-full flex flex-col justify-center items-center">
      <Slider />
    </main>
  );
}

export default Home;
